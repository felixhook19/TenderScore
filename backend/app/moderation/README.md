# `app/moderation`

Tiered queue (escalated first), confirm/amend with mandatory rationale on amend (DB-enforced), decision audit trail. Built in M7.

Tests: see `tests/` (unit, integration, regression and red-team suites cover this module; the audit-completeness walk exercises its endpoints).
