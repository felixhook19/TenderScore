# `app/documents`

Moderation pack generation strictly from the moderation record (docx via python-docx, pdf via reportlab), versioned, hashed, stored. Built in M8.

Tests: see `tests/` (unit, integration, regression and red-team suites cover this module; the audit-completeness walk exercises its endpoints).
