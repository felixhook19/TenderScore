# `app/scoring`

Per question x bidder runs in strict isolation; multi-pass at temperature 0 with seeded deterministic requirement ordering; citation validation in code; variance routing; calibration gate; gate blocking; replay from the record. Built in M5.

Tests: see `tests/` (unit, integration, regression and red-team suites cover this module; the audit-completeness walk exercises its endpoints).
