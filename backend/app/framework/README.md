# `app/framework`

Procurements, lots, the criteria tree (sub-criteria first-class), verbatim band descriptors, gates and limits, the reproducible framework lock that pins the model version, and the LLM-assisted extraction draft. Built in M3.

Tests: see `tests/` (unit, integration, regression and red-team suites cover this module; the audit-completeness walk exercises its endpoints).
