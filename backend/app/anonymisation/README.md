# `app/anonymisation`

Rules + gazetteer redaction (bidder register, emails, phones, postcodes, company numbers), token assignment (Bidder A...), and the privileged reveal where every access is individually audited. Built in M6.

Tests: see `tests/` (unit, integration, regression and red-team suites cover this module; the audit-completeness walk exercises its endpoints).
